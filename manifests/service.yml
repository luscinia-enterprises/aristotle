apiVersion: v1
kind: Service
metadata:
    name: "aristotleplatform-6596"
    labels:
        app: "aristotleplatform-6596"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aristotleplatform-6596"